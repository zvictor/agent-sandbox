## nsc auth trust-relationships add

Add a new trust relationship.

### Synopsis

Add a new trust relationship by specifying issuer and subject match patterns.

```
nsc auth trust-relationships add [flags]
```

### Options

```
      --audience string                 Expected audience value.
      --default_token_duration string   Default token duration (e.g. "3600s").
      --grant stringArray               Grant default permission as JSON object (can be specified multiple times). Format: {"resource_type":"...","resource_id":"...","actions":["..."]}
  -h, --help                            help for add
      --issuer string                   Token issuer (required).
      --subject-match string            Subject match pattern (required).
```

### SEE ALSO

* [nsc auth trust-relationships](nsc_auth_trust-relationships.md)	 - Manage trust relationships for authentication.

###### Auto generated by spf13/cobra on 8-Apr-2026
