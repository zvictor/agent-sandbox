## nsc cache sccache setup

Set up a remote sccache cache and output the required environment variables.

### Synopsis

Set up a remote sccache cache and output the required environment variables.

This command provisions a remote build cache and outputs the environment
variables needed to configure sccache to use it.

The output includes:
  SCCACHE_WEBDAV_ENDPOINT - The WebDAV endpoint URL
  SCCACHE_WEBDAV_KEY_PREFIX - The key prefix path
  SCCACHE_WEBDAV_TOKEN - The authentication token

```
nsc cache sccache setup [flags]
```

### Options

```
      --cache_name string   A name for the cache.
  -h, --help                help for setup
  -o, --output string       One of plain or json. (default "plain")
      --site string         Site preference (e.g., 'iad', 'fra'). If not set, determined automatically.
      --token string        Use the bearer token stored at this location for authentication instead of the default.
```

### SEE ALSO

* [nsc cache sccache](nsc_cache_sccache.md)	 - sccache cache related functionality.

###### Auto generated by spf13/cobra on 8-Apr-2026
