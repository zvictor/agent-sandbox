## nsc logs

Print logs for an instance, or across all instances with --all.

### Synopsis

Print application logs for an instance. Use --all with --since, --after, or --before to query logs across all instances.

```
nsc logs [instance-id] [flags]
```

### Options

```
      --after string                 Only show logs after this timestamp (RFC3339, e.g. 2024-01-15T10:30:00Z).
      --all                          Search across all instances. Requires --since, --after, or --before.
      --before string                Only show logs before this timestamp (RFC3339, e.g. 2024-01-15T12:00:00Z).
  -c, --container string             If specified, only display logs of this container.
  -f, --follow                       Specify if logs should be streamed continuously.
  -h, --help                         help for logs
  -k, --kind strings                 If specified, only display logs of these kinds (e.g. kubernetes, containers, system, applications). Can be specified multiple times or comma-separated.
      --limit int32                  Maximum number of log lines to return. (default 1000)
      --match_message_regex string   If specified, only display logs whose message matches this regular expression.
  -n, --namespace string             If specified, only display logs of this Kubernetes namespace.
  -o, --output string                Output format. Supported values: plain, json (outputs one JSON object per line). (default "plain")
  -p, --pod string                   If specified, only display logs of this Kubernetes Pod.
      --raw                          Output raw logs (skipping namespace/pod labels).
      --since duration               Show logs relative to a timestamp (e.g. 42m for 42 minutes). The flag can't be use with --follow. (default 0s)
  -s, --source string                If specified, display logs from this source. Default: kubernetes (default "kubernetes")
```

### SEE ALSO

* [nsc](nsc.md)	 - 

###### Auto generated by spf13/cobra on 8-Apr-2026
