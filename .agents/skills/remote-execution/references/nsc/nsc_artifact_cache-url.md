## nsc artifact cache-url

Download an arbitrary URL using a pull-through cache.

### Synopsis

Download the content from an arbitrary URL and cache it for fast access.

If the content is already present in the artifact cache for the given URL it will be used instead.

The content at the URL is assumed to be immutable.

```
nsc artifact cache-url [target url] --out=[filename] { --max_age=[duration] } [flags]
```

### Options

```
  -h, --help               help for cache-url
      --max_age duration   Redownload from source if the cached content is older than this duration. (default 0s)
      --out string         Filename to save the downloaded content at.
```

### SEE ALSO

* [nsc artifact](nsc_artifact.md)	 - Artifact-related activities.

###### Auto generated by spf13/cobra on 8-Apr-2026
