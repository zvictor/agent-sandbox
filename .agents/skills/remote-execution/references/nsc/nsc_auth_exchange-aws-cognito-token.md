## nsc auth exchange-aws-cognito-token

Generate a Namespace Cloud token from a AWS Cognito JWT.

```
nsc auth exchange-aws-cognito-token [flags]
```

### Options

```
      --aws_profile string     Use the specified AWS profile.
      --aws_region string      The AWS region to connect to.
      --ensure duration        If the current token is still valid for this duration, do nothing. Otherwise fetch a new token. (default 0s)
  -h, --help                   help for exchange-aws-cognito-token
      --identity_pool string   UUID of the identity pool.
      --tenant_id string       What tenant to authenticate.
```

### SEE ALSO

* [nsc auth](nsc_auth.md)	 - Authentication related operations (e.g. login).

###### Auto generated by spf13/cobra on 8-Apr-2026
