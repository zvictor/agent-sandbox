## nsc cache bazel setup

Set up a remote Bazel cache and generate a bazelrc to use it.

```
nsc cache bazel setup [flags]
```

### Options

```
      --bazelrc string                   If specified, write the bazelrc to this path.
  -h, --help                             help for setup
  -o, --output string                    One of plain or json. (default "plain")
      --static                           If specified, use a static bearer token in --remote_header instead of a credential helper.
      --static_token_duration duration   The minimum duration of the static token configured (requires --static). (default 4h0m0s)
```

### SEE ALSO

* [nsc cache bazel](nsc_cache_bazel.md)	 - Bazel cache related functionality.

###### Auto generated by spf13/cobra on 8-Apr-2026
