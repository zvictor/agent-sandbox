## nsc create

Creates a new instance.

```
nsc create [flags]
```

### Options

```
      --bare                        If set to true, creates an environment with the minimal set of services (e.g. no Kubernetes).
      --cidfile string              If specified, write the instance id to this path.
      --duration duration           For how long to run the ephemeral environment. (default 0s)
      --enable strings              Enable a feature, e.g. --enable=kubernetes:1.33
      --ephemeral                   Create an ephemeral instance.
      --experimental string         JSON definition of experimental features.
      --experimental_from string    Load experimental definitions from the specified file.
      --features strings            A set of features to attach to the instance.
  -h, --help                        help for create
      --ingress string              If set, configures the ingress of this instance. Valid options: wildcard.
      --label stringToString        Key-values to attach to the new instance. Multiple key-value pairs may be specified. (default [])
      --machine_type string         Specify the machine type.
  -o, --output string               One of plain or json. (default "plain")
      --output_json_to string       If specified, write instance metadata as JSON to this path.
      --output_registry_to string   If specified, write the registry address to this path.
      --purpose string              What documented purpose to attach to the created instance. (default "Manually created from CLI")
      --selectors strings           Select platform/base image based on specific properties (prop1=value1,prop2=value2).
      --unique_tag string           If specified, creates a instance with the specified unique tag.
      --volume strings              Attach a volume to the instance, {cache|persistent}:{tag}:{mountpoint}:{size}
      --wait_kube_system            If true, wait until kube-system resources (e.g. coredns and local-path-provisioner) are ready.
      --wait_timeout duration       For how long to wait until the instance becomes ready. (default 1m0s)
```

### SEE ALSO

* [nsc](nsc.md)	 - 

###### Auto generated by spf13/cobra on 8-Apr-2026
