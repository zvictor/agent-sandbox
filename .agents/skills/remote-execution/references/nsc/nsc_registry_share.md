## nsc registry share

Share an image publicly or with partner accounts.

### Synopsis

Share an image publicly or with partner accounts.

The image reference can be provided as a positional argument in the format:
  - repository:tag (e.g., myrepo:latest)
  - repository@digest (e.g., myrepo@sha256:abc123...)

Alternatively, use --repository and --digest flags.

```
nsc registry share [image-reference] [flags]
```

### Options

```
      --digest string       Image digest
      --expires_at string   Expiration time in RFC3339 format (e.g., 2024-12-31T23:59:59Z)
  -h, --help                help for share
  -o, --output string       Output format: table, json (default "table")
      --repository string   Repository name
      --suffix string       An optional suffix to append to repository names
      --visibility string   Visibility level: public, partner (default "public")
```

### SEE ALSO

* [nsc registry](nsc_registry.md)	 - Manage container registry images.

###### Auto generated by spf13/cobra on 8-Apr-2026
