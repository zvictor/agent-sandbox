## nsc registry policy set

Sets policy settings.

### Synopsis

Sets the policy settings. If --repository is specified, sets the policy for that repository, otherwise sets the default policy.

```
nsc registry policy set [flags]
```

### Options

```
      --default_expiration duration   The expiration duration applied to new images (e.g., 168h for 7 days). Needs to be at least 4h. (default 0s)
  -h, --help                          help for set
      --no-expiration                 Set images to never expire.
      --repository string             Set the policy for a specific repository.
```

### SEE ALSO

* [nsc registry policy](nsc_registry_policy.md)	 - Manage registry policies.

###### Auto generated by spf13/cobra on 8-Apr-2026
