#!/usr/bin/env bash
set -euo pipefail

root_dir="$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)"
for arg in "$@"; do
  if [ "$arg" = "--dangerously-skip-permissions" ]; then
    exec "$root_dir/bin/agent" claude "$@"
  fi
done

exec "$root_dir/bin/agent" claude --dangerously-skip-permissions "$@"
